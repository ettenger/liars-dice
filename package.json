{
  "name": "liars-dice",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start": "npm run lint && npm run build && node dist",
    "clean": "rm -rf dist && mkdir dist",
    "build": "npm run clean && tsc",
    "lint": "tslint --force -c tslint.json 'src/**/*.ts'",
    "test": "jest"
  },
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "dotenv": "^8.2.0",
    "lodash": "^4.17.15",
    "typescript": "^3.8.2",
    "ws": "^7.2.3"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.149",
    "@types/node": "^13.7.4",
    "jest": "^25.1.0",
    "ts-jest": "^25.2.1"
  }
}
